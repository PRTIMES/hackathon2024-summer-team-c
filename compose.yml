services:
  api:
    build: ./api
    ports:
      - "80:80"
    environment:
      - ENV=production
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    command: uvicorn src.main:app --reload --host 0.0.0.0 --port 80

